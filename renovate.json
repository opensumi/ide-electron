{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base"], 
  "timezone": "Asia/Shanghai",
  "enabledManagers": ["npm"],
  "packageRules": [
    {
      "packagePatterns": ["*"],
      "excludePackagePatterns": ["^@opensumi/ide-"],
      "enabled": false,
      "groupName": ["opensumi packages"],
      "addLabels": ["OpenSumi Version"],
      "commitMessage": "{{semanticPrefix}}{{#if isPin}}Pin{{else}}Update{{/if}} `@opensumi/ide-` to version {{newVersion}} 🌟"
    }
  ]
}
